<h1>Make your mark</h1>

<%= form_for([@journey, @claim]) do |f| %>
  <% if @claim.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@claim.errors.count, "error") %> prohibited this claim from being saved:</h2>

      <ul>
      <% @claim.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<%= f.file_field :validation, class: "btn btn-primary" %>
  <%= f.submit "Upload your image", class: "btn btn-lg btn-primary btn-block" %>
<% end %>

